{
  "name": "gulp-merge-streams",
  "version": "1.0.0",
  "description": "Gulp merge streams sample project",
  "main": "gulp.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "event-stream": "^4.0.1",
    "glob-parent": "^5.1.2",
    "gulp": "^5.0.0",
    "merge-stream": "^2.0.0"
  },
  "overrides": {
    "gulp": {
      "vinyl-fs": {
        "glob-stream": {
          "glob-parent": "^5.1.2"
        }
      },
      "glob-watcher": {
        "chokidar": {
          "glob-parent": "^5.1.2"
        }
      }
    }
  }
}
